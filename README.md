# securedatatransmission
Accepts a message from  user. Creates a SHA-256 hash encrypts the message decrypts it and compares hash to verify everything. Uses encryption so the message cannot be easily read by anyone for confidentiality. For integrity a SHA-256 hash before and after encryption. For availability let users encrypt decrypt and verify messages whenever its on. 
